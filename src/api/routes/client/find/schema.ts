import { type Schema } from 'express-validator';

const findClientsSchema: () => Schema = () => ({
  // Empty schema for now, can add pagination/filter params later
});

export default findClientsSchema;
